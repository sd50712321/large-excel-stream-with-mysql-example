//- header
nav(class='main-header navbar navbar-expand navbar-dark navbar-teal')
    ul(class='navbar-nav')
        li(class='nav-item')
            a(class='nav-link' data-widget='pushmenu', href='/', role='button')
                i(class='fas fa-bars')
        li(class='nav-item')
            if session            
                strong(class='nav-link') #{session.camp_admin_id} 님 환영합니다!
            else
                strong(class='nav-link') 관리자 님 환영합니다!
    ul(class='navbar-nav ml-auto')
        li(class='nav-item')
            a(class='nav-link', id='currentTimer')
        li(class='nav-item')
            a(class='nav-link' href='/logout') 로그아웃
        li(class='nav-item')
            a(class='nav-link', role='button')
                i(class='fas fa-th-large')